name: Generate Final Recap AI Phrases

on:
  workflow_dispatch:

jobs:
  disabled-until-final:
    runs-on: ubuntu-latest

    steps:
      - name: AI generation is intentionally disabled
        run: |
          echo "AI generation is disabled for v39 final recap testing."
          echo "Use Audit Final Recap Data first."
          echo "Final AI phrase generation will be enabled only after tournament data is locked."
          exit 1
